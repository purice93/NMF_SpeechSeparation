

语音分离的分类：
	干扰为噪声时：语音增强
		使用IBM，理想二值掩蔽
		基于频谱映射
	干扰为说话人：语音分离
	干扰为自己的回声：解混响

目标：为了更好地进行语音识别


基于深度学习的语音分离的方法：
	DNN、LSTM长短时记忆、CNN
	GAN
	
训练目标：
	基于mask的方法：（即分类问题）
		IBM（理想二值掩蔽）：通过计算某一个时频单元的信噪比，大于某个值设为1，小于设为0
		IRM（理想浮值掩蔽）：计算信号和噪声的能量比，结果结果介于0-1之间
		3、4、5
	基于频谱衍射的方法：（回归问题）